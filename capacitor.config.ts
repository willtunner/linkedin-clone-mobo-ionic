import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'linkedin-clone-mobo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
